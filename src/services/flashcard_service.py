async def generate_flashcards_from_text(text: str) -> list:
    """
    Business logic to generate flashcards from text.
    """
    # TODO: Implement actual flashcard generation logic with LLM
    print(f"Generating flashcards from text: {text[:50]}...")
    return [{"question": "What is the capital of France?", "answer": "Paris"}]
