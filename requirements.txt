# --- Flask + App Layer ---
flask
flask-cors
flask-login
Flask-Babel
werkzeug

# --- Server (Production) ---
gunicorn

# --- Background Workers / Messaging ---
celery
pika
tenacity
schedule

# --- Database + GridFS ---
pymongo

# --- AI Clients ---
openai
google-generativeai
httpx
authlib

# --- Configuration / Validation ---
pydantic
pydantic-settings
python-dotenv

# --- Networking / Utils ---
requests
rich
python-json-logger
psutil

# --- File Processing + Parsing ---
python-magic-bin; sys_platform == "win32"
python-magic; sys_platform != "win32"
beautifulsoup4
markdown
pypdf2
python-docx
python-pptx

# Tesseract OCR (library only; binary installed separately)
pytesseract
Pillow

# --- Authentication ---
bcrypt
email-validator

# --- PDF / DOCX rendering (HTML → PDF) ---
weasyprint
